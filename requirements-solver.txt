# Optional SMT layer (WP-15). Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-solver.txt
# A pyproject.toml `[solver]` extra would mirror this line; none exists yet under
# rule_navigation/ — add packaging there before wiring an extra alias.
z3-solver
